spring:
  application:
    name: config-service

  profiles:
    active: native

  cloud:
    config:
      server:
        native:
         searchLocations: file:///${user.home}/git/streamline-toolbox/configs,file:///configs
